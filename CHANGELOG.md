# Changelog

All notable changes to this project will be documented in this file.

## [2.1.0] - 2026-09-19

### 🕊️ Release: Чистый швейцарский минимализм (vibeprompts.dev / Linear) & Искоренение «AI-Slop»

- **Hero Screen (Полная ликвидация псевдо-киберпанка и AI-штампов)**:
  - Устранены вращающиеся SVG-радары, пульсирующие неоновые круги, псевдо-технологичные надписи «TELEMETRY ONLINE» и размытые цветные пятна.
  - Внедрен строгий минималистичный дизайн в духе `vibeprompts.dev` и Linear:
    - Статусный бейдж: аккуратный цинковый чип с точечным зеленым индикатором (`.hero-minimal-badge .status-dot`).
    - Четкая, лаконичная типографика (H1 и подзаголовок без аляповатых градиентов).
    - Минималистичные кнопки действия: контрастная монохромная `.btn-solid-white` («Вызвать мастера») и контурная `.btn-outline-zinc` («Оставить заявку»).
    - 4-факторная полоса метрик в карточках строгого цинкового стиля (`.metric-item`): опыт 12+ лет, 3500+ авто, гарантия 12 месяцев, диагностика от 800 ₽.
  - Сохранены все динамические привязки данных (`hero-title`, `hero-subtitle`, `hero-accepting-text`, `hero-call-btn`, `hero-request-btn`, `hero-bubble-price`, `hero-city`).

- **Каталог услуг (Отказ от бесконечного «матраса» в пользу 6 ключевых услуг + спойлер)**:
  - Ликвидирована растянутая сетка 22 карточек на первом экране, перегружавшая мобильный интерфейс.
  - На главной странице по умолчанию отображаются ровно 6 ключевых услуг с прямыми ценами (от 800 ₽ до 1 500 ₽):
    1. Компьютерная диагностика ЭБУ (от 800 ₽)
    2. Выездной запуск / АКБ (от 1 500 ₽)
    3. Поиск утечки тока и КЗ (от 1 000 ₽)
    4. Ремонт генератора и стартера (от 1 500 ₽)
    5. Отключение сигнализаций / иммо (от 1 500 ₽)
    6. Восстановление проводки (от 1 500 ₽)
  - Добавлена элегантная кнопка раскрытия каталога: `[ Показать все 22 услуги ▾ ]` (`#toggle-services-btn`).
  - При раскрытии отображается полный список услуг с мгновенным поиском (`#service-search`) и кнопками быстрого заказа в модальном окне.

- **Калькулятор неисправностей (SaaS Checkout Style)**:
  - Устранен перегруженный HUD с неоновыми рамками.
  - Внедрен строгий 3-шаговый конфигуратор неисправности (`.calc-clean-card`): категория авто → характер поломки → расчет и мгновенный заказ.
  - Бесшовная передача параметров неисправности в модальную форму оформления заявки.

- **Инфраструктура и производительность**:
  - Обновлены версии кэш-бастеров (`?v=2.1.0`) в `public/index.html`.
  - Подтверждена маршрутизация домена `чекгорит.рф` на Beget DNS (A-запись на Anycast IP Vercel `76.76.21.22`, статус HTTP 200 OK, SSL валиден).

## [2.0.0] - 2026-09-19

### ⚡ Major Release: Радикальный редизайн 2026 года (Мультиагентная архитектура на Groq LPU)

- **Hero HUD Vector Canvas (Zero Raster Images / LCP < 200ms)**:
  - Полный отказ от растровых тяжелых фотографий в первом экране сайта (`#hero`).
  - Внедрен чистый векторный кибер-канвас на SVG Data-URI с тонкой координатной сеткой HUD, анимированным радарным сканером телеметрии (`radar-scanner`), вращающимся лучом сканирования и пульсирующим центром.
  - Интегрирована консоль показателей мастера: 12+ лет опыта, 3500+ запущенных авто, базовые цены от 800 ₽ и дилерские сканеры.
  - Все динамические связки (статус «Свободен/Занят», телефон, кнопка заявки, город) сохранены.

- **Bento Services Matrix (Каталог 22 услуг без «матрасов»)**:
  - Ликвидирован старый громоздкий двухколоночный макет («матрасы»), растягивавший мобильную страницу.
  - Внедрена адаптивная Bento-матрица на CSS Grid (`.bento-grid`) со стильными стеклянными плитками (`glassmorphism`), векторными иконками Lucide и бейджами цен.
  - Добавлен живой поиск по 22 услугам с дебаунсом (`#service-search`).
  - Внедрены быстрые категорийные фильтры-чипсы (`#service-filters`: Все, Диагностика, Пуск & АКБ, Проводка & Блоки, Сигнализации & Иммо, Свет & Комфорт), которые аккуратно переносятся (wrap) без горизонтального скролла и обрезания на мобильных устройствах.
  - В каждую карточку встроен микро-аккордеон с детальным описанием и кнопкой прямого заказа услуги в модальном окне с предзаполненным названием.

- **Интерактивный бортовой конфигуратор неисправностей (HUD Configurator)**:
  - Калькулятор переработан в 4-шаговый интерактивный конфигуратор экспресс-диагностики:
    - Шаг 1 (Автомобиль): Отечественный, Иномарка до 2016, Свежая иномарка / Премиум (VAG, BMW, Mercedes, LiXiang/Geely), Коммерческий транспорт.
    - Шаг 2 (Симптом): 6 ключевых неисправностей с кибер-иконками (стартер, АКБ/генератор, сигнализация, КЗ/проводка, Check Engine/ЭБУ, свет/комфорт).
    - Шаг 3 (Локация и срочность): Выбор зоны (КАД СПб, до 20 км, трасса/область) и тумблер приоритетного экстренного выезда мастера (15–20 мин).
    - Шаг 4 (Результат): Расчет ориентировочной стоимости, времени в пути мастера и кнопка мгновенного вызова мастера с передачей полной сводки телеметрии в форму заявки.

- **Дизайн-система 2026**:
  - Палитра: кибер-графит `#07090e`, стекло `#0d111a` с `backdrop-filter`, неоновый циан `#00f0ff`, сигнальный янтарь `#ffb800`.
  - Обновлены версии кэш-бастеров (`?v=2.0.0`) для CSS и JS скриптов.

## [1.7.0] - 2026-09-19

### 🏆 Release: Полное завершение и закрытие Этапа 3 (Клиентский опыт и Omnichannel Identity)

- **Бесшовная изоляция экранов заявки на лендинге (`public/index.html`)**:
  - Все поля ввода и кнопки модального окна заявки объединены в контейнер `#req-fields-group`. При отправке заявки форма скрывается полностью, устраняя остаточные текстовые метки «Имя», «Телефон», «Опишите проблему» над карточкой успешного оформления.
- **Инлайн-валидация и реактивное слияние клиентов в CRM (`admin.js`)**:
  - В модальное окно создания ремонта добавлен информативный инлайн-баннер `#repair-error-banner`.
  - При конверсии входящей заявки в ремонт, если клиент с таким номером телефона уже существует в базе, CRM автоматически обновляет список клиентов через `loadClients()`, связывает создаваемый ремонт с уже существующей карточкой клиента по нормализованному номеру (сравнение 10 последних цифр) и предотвращает блокировку ошибки `client_exists`.
- **Восстановление системных связей лид-аккаунта Sid Vicious**:
  - После завершения успешных приемочных тестов Этапа 3 восстановлены официальные привязки аккаунта Главного мастера в базе данных Supabase (`vk_id: 1125744855`, `telegram_id: 6208918931`, `telegram_username: @Tri_chasa_nazad`, `phone: +79965232109`).

## [1.6.1] - 2026-09-19

### 🤖 Fix: Разделение сессий клиента и Telegram-бота (Level 2 Omnichannel Auth)

- **Интеграция модального окна входа через Telegram-бота (Level 2 Deep Link)**:
  - В `public/profile.html` и `public/js/profile.js` добавлена возможность выбора способа входа через Telegram: классический браузерный Web OAuth или нативный Telegram-бот (`@Autoelectrical_Official_bot`).
  - Внедрена генерация защищенного одноразового токена через `GET /api/client/auth/telegram/magic` с отображением 6-значного кода и диплинка бота `t.me/...?start=auth_<session>`, с фоновым поллингом статуса подтверждения.
- **Изоляция режима разработчика (`isDevUser`)**:
  - Устранена ложная активация панели управления разработчика: `isDevUser()` теперь строго проверяет системный идентификатор руководителя (`19e4e551-4454-4710-8a6d-a4effc211201`) или системные роли (`lead`/`admin`), исключая навязывание dev-панели обычным клиентам из-за наличия токена администратора в `localStorage`.

## [1.6.0] - 2026-09-19

### 🚀 Release: Завершение и закрытие Этапа 2 (Операционное удобство мастера и CRM Workflow)

- **Восстановление и инициализация хранилища медиа-файлов Supabase Storage (`uploads`)**:
  - Создан публичный бакет `uploads` в Supabase Storage, устранён сбой сохранения прикрепляемых фото к ремонтам. Фотографии теперь корректно отображаются в таймлайне ремонта и открываются в полном размере.
- **Синхронизация расчёта даты последнего визита и Churn-контроля**:
  - Исправлен расчёт `lastRepairDate` на фронтенде в `renderClients` (`admin.js`) и `renderStats` (`profile.js`): дата последнего визита теперь вычисляется строгой сортировкой валидных ремонтов по убыванию даты, устраняя ложное окрашивание активных клиентов в красный цвет.
- **Фоновый авто-опрос входящих заявок (Live Request Auto-Sync)**:
  - Внедрён механизм периодического фонового опроса `initRequestsAutoSync` (каждые 25 секунд и при возврате на вкладку через `visibilitychange`), гарантирующий мгновенную реактивность пульсирующего бейджа `#new-requests-badge` без ручной перезагрузки F5.
- **Расширение живого поиска клиентов**:
  - В `renderClients` добавлен поиск по UUID клиента (`c.id`), 4-значному ПИН-коду (`c.pin`) и нормализованным номерам телефонов (с автоматической очисткой пробелов и скобок).
- **Антиспам-защита на создание заявок**:
  - В `POST /api/requests` добавлен 2-минутный кулдаун на создание повторных заявок с одного и того же номера телефона со статусом `new` (HTTP 429).
- **Улучшение интеграции с WhatsApp**:
  - В функции шеринга `shareClientLink` добавлена авто-нормализация российских номеров (`8...` ➔ `7...`), позволяющая открывать прямой диалог в `wa.me` даже с клиентами, не сохранёнными в телефонную книгу мастера.

## [1.5.1] - 2026-09-19

### 🔄 Auto-Sync: Динамическая синхронизация версии Dev Center с `package.json`

- **Автоматическое обновление бейджа версии:**
  - Бейдж версии в HUD Dev Center (`#dev-app-version`) теперь динамически привязан к серверной телеметрии `/api/dev/vitals`.
  - Бэкенд считывает актуальную версию напрямую из `package.json` (`APP_VERSION`).
  - Тестовые PUSH-уведомления в Telegram (`/api/dev/test-push`) также автоматически включают актуальный номер версии из `package.json`.
  - Исключена вероятность рассинхронизации версий между кодом, версией релиза и интерфейсом панели.

## [1.5.0] - 2026-09-19

### ⚡ Feature: Панель управления разработчика и руководителя (Developer & Lead Control Center)

- **Индивидуальное пространство разработчика (эксклюзивно для Sid Vicious / Lead Master)**:
  - Профиль главного мастера `id: 19e4e551-4454-4710-8a6d-a4effc211201` трансформирован в командный центр мониторинга и телеметрии.
  - Рядовые клиенты по-прежнему видят только чистый клиентский гараж и историю ремонтов.
  - В шапке профиля для руководителя доступен интерактивный тумблер `[ ⚡ Управление ] <---> [ 👤 Клиент ]` для мгновенного предпросмотра клиентского интерфейса.
- **Инженерная телеметрия и радар сервисов в реальном времени**:
  - `GET /api/dev/vitals`: мониторинг реальной задержки Supabase PostgreSQL (ms), пинга Telegram Bot API (ms), использования оперативной памяти Node.js (`heapUsedMb`, `heapTotalMb`), аптайма и Edge TTFB.
  - Сводка сущностей БД: живые счётчики базы данных (клиенты, заявки, мастера, автомобили, ремонты).
  - Консоль событий сервера (`Live Log Console`) с выводом последних системных логов из буфера памяти.
- **Командная палуба и самодиагностика**:
  - `POST /api/dev/diagnostics`: полный стресс-тест инфраструктуры (PostgreSQL, Telegram Bot, криптографический движок JWT, буфер логов) с детализированным отчётом и индикаторами задержки.
  - `POST /api/dev/test-push`: мгновенная отправка форматированного отчёта телеметрии в личный Telegram руководителя (`@Tri_chasa_nazad`, Chat ID `6208918931`).
  - `POST /api/dev/clear-cache`: горячий сброс кеша токенов, информации о боте и очистка оперативного буфера логов.
  - Быстрый переход в CRM-панель мастера (`/admin.html`) в один клик.

## [1.4.7] - 2026-09-19

### 👑 Fix: Привязка Telegram-авторизации к системному аккаунту Lead / Главного мастера (Sid Vicious)

- **Бесшовная идентификация учетной записи руководителя**:
  - В `/api/client/auth/telegram` добавлена проверка наличия Telegram ID в таблице мастеров `masters` перед созданием новой записи клиента.
  - Если Telegram ID совпадает с `telegram_chat_id` мастера (Sid Vicious), сессия автоматически привязывается к постоянному системному профилю `id: 19e4e551-4454-4710-8a6d-a4effc211201` вместо генерации дубликата клиента.
  - В базе данных Supabase обновлены поля `telegram_id` и `telegram_chat_id` для профиля `Sid Vicious`, удалена временная запись-дубликат.

## [1.4.6] - 2026-09-19

### ⚡ Fix: Telegram In-Browser OAuth Callback & Popup Handshake Fix

- **Обработка ответа официального веб-OAuth Telegram**:
  - Исправлен перехват `postMessage` от `oauth.telegram.org`: добавлен парсинг структуры `data.auth_data` (официальный формат Telegram `auth_user`), а также fallback на `data.result`, `data.user` и прямой объект данных.
  - Добавлена поддержка передачи данных через `return_to` хеш (`#tgAuthResult=<base64>`) с автоматическим декодированием и уведомлением родительского окна через `window.opener.postMessage`.
  - Устранено зависание всплывающего окна после нажатия кнопки «Принять» в окне авторизации Telegram.
  - Предотвращена блокировка кнопки «Войти через Telegram» при повторных попытках входа.

## [1.4.5] - 2026-09-19

### 🚫 Complete Elimination of Telegram Bot Flow for Clients (Zero Desktop Apps, Zero /start)

- **Полное исключение перехода в Telegram-бота**:
  - Полностью удален вызов протокола `tg://resolve`, запускавшего приложение Telegram Desktop и открывавшего диалог с ботом с кнопкой «Запустить».
  - Удалены подсказки «Ожидаем подтверждения в боте...» и любые ссылки на бот-чат для клиентов.
- **Чистый веб-OAuth Telegram**:
  - При клике по кнопке «Войти через Telegram» открывается отдельное всплывающее браузерное окно авторизации `oauth.telegram.org` (или прямой редирект), где пользователь подтверждает вход непосредственно в браузере.
  - Клиент больше никогда не видит бота, не попадает в чат и не нажимает техническую команду `/start`.

## [1.4.4] - 2026-09-19

### 🛡️ Fix & UI: Восстановление постоянных ярких кнопок входа через Telegram и ВКонтакте

- **Полноценная кнопка «Войти через Telegram»**:
  - Устранена проблема пустого экрана из-за провайдерской блокировки скрипта `telegram.org`: кнопка Telegram теперь является постоянным, ярким элементом интерфейса (фирменный синий цвет `#0088CC` с логотипом).
  - При клике инициируется сессия и открывается нативное приложение Telegram через протокол `tg://resolve?domain=Autoelectrical_Official_bot&start=auth_<sessionId>`, минуя блокируемый веб-домен `t.me`.
  - Появляется компактный статус ожидания подтверждения и ссылка повторного открытия при необходимости.
- **Полноценная кнопка «Войти через ВКонтакте»**:
  - Кнопка VK ID (фирменный цвет `#0077FF`) выведена на экран на постоянной основе и привязана напрямую к серверному эндпоинту `/api/client/auth/vk/login`.
  - Вход через ВКонтакте работает безотказно без зависимости от внешних CDN-скриптов и сторонних виджетов.

## [1.4.3] - 2026-09-19

### ⚡ Feature & UI/UX: «Тихая» авторизация Telegram Login Widget, очистка экрана входа и Демо-песочница клиента

- **Официальный Telegram Login Widget**:
  - Интегрирован официальный скрипт `https://telegram.org/js/telegram-widget.js?22` с привязкой к зарегистрированному в `@BotFather` домену `xn--c1adkgvmp7a.xn--p1ai` (`чекгорит.рф`).
  - Обеспечена настоящая «тихая» авторизация без необходимости переходить в чат бота, писать сообщения или нажимать команду `/start`.
  - Авторизация выполняется через штатное модальное окно Telegram OAuth в 1 клик с последующей валидацией HMAC-SHA256 подписи на бэкенде (`POST /api/client/auth/telegram`).
- **Очистка экрана логина от дублирующих и устаревших элементов**:
  - Удалена тяжеловесная динамическая вставка блока с обратным отсчётом 10:00, второй кнопкой и статусами ожидания, создававшая визуальный шум.
  - Удалено дублирование кнопки ВКонтакте: One Tap виджет остаётся основным аккуратным способом входа, а запасная кнопка прямого входа скрыта и активируется автоматически только при сбое загрузки One Tap SDK.
  - Заменена аварийная внешняя web-ссылка `https://t.me/...` (вызывавшая таймауты `ERR_CONNECTION_TIMED_OUT` из-за сетевых блокировок) на прямую нативную ссылку протокола приложения `tg://resolve?domain=Autoelectrical_Official_bot&start=...`.
- **1-клик Демо-песочница клиента (`POST /api/client/auth/demo`)**:
  - Добавлена кнопка «🎭 Войти как тестовый клиент (демо-гараж)», позволяющая моментально протестировать интерфейс клиента (2 автомобиля, завершённые ремонты с суммами и гарантией, напоминание о ТО, уровень лояльности со скидкой) без необходимости иметь второй номер телефона или аккаунт Telegram.
  - Полная изоляция от учётной записи `lead` мастера `Sid Vicious`: тестирование клиентской части теперь абсолютно автономно и безопасно.
- **Улучшенный поиск клиента в `POST /api/client/auth/telegram`**:
  - Поиск связки теперь учитывает как `telegram_id`, так и `telegram_chat_id`, с автоматической синхронизацией актуального никнейма Telegram.

## [1.4.2] - 2026-09-19

### 🎨 UI/UX: VibePrompt Empty States для списков и усиление визуализации бейджа заявок

- **CRM: Интерактивные состояния «Ничего не найдено» (Empty States)**:
  - В разделе «Клиенты» (`renderClients`) реализован дизайн Empty State по стандарту VibePrompt: при пустом поисковом запросе выводится карточка с иконкой `search-x`, цитатой запроса и кнопкой «Сбросить поиск», а при пустой базе — приглашение создать первого клиента.
  - В разделе «Заявки» (`renderRequests`) при отсутствии необработанных обращений выводится карточка с иконкой `inbox` и кнопкой быстрого ручного добавления заявки.
- **Визуализация счётчика заявок в шапке**:
  - На бейдж `#new-requests-badge` добавлены классы `bg-red-500 text-white`, обеспечивающие контрастность и выразительную пульсацию при появлении новых входящих заявок.

## [1.4.1] - 2026-09-19

### 🛡️ Feature & Security: Информативная карточка сотрудника и многоуровневая защита системных аккаунтов (`lead` / `chief`)

- **Карточка сотрудника (`Карточка мастера`) в CRM**:
  - Вместо устаревшей компактной таблицы внедрена адаптивная сетка карточек сотрудников (`#masters-cards-container`) с аватарами инициалов, статусом защиты, бейджами ролей, кликабельными телефонами и специализацией.
  - Разработано интерактивное модальное окно карточки сотрудника (`#modal-master-card`) с полным профилем:
    - ФИО / отображаемое имя сотрудника;
    - Системный логин (`username`) для авторизации в CRM;
    - Должность и роль (`lead` — CEO / Руководитель, `chief` — Главный автоэлектрик / Проект Лид, `master` — Мастер-автоэлектрик, `apprentice` — Подмастерье / Помощник, `manager` — Клиентский сервис / Менеджер);
    - Прямой контактный телефон мастера;
    - Специализация и ключевые компетенции (диагностика, генераторы, сигнализации, чип-тюнинг и т.д.);
    - Служебные заметки руководителя (график смен, рабочая зона, закрепленный инструмент);
    - Блок статуса интеграции с Telegram-ботом (`Chat ID`, подсказка по привязке через команду `/admin [пароль]`);
    - Безопасная смена пароля сотрудника прямо из карточки (минимум 6 символов, bcrypt 10 раундов).
- **Многоуровневая система безопасности аккаунтов (Security Core)**:
  - **Защита аккаунтов уровня `lead` и `chief`**:
    - Идентификаторы `19e4e551-4454-4710-8a6d-a4effc211201` (Sid Vicious) и `41dbea03-a28c-4d1e-bbf0-be86737301b5` (Seryozhka Abisonov), а также логины `vk_1125744855` и `vk_250130315` защищены на уровне бэкенда от удаления (возврат HTTP 403 с аудит-логом).
    - Защита от понижения в должности (`demotion block`): роли `lead` и `chief` жестко зафиксированы в бэкенде и не могут быть сброшены через API.
    - Блокировка изменения системного логина (`username lock`) для защищённых профилей.
  - **Разграничение прав доступа (RBAC)**:
    - Создавать новых сотрудников (`POST /api/masters`) и удалять сотрудников (`DELETE /api/masters/:id`) имеют право только руководители (`lead` и `chief`).
    - Обычные мастера (`master`, `apprentice`, `manager`) не могут удалять коллег или назначать кому-либо привилегированные роли `lead`/`chief`.
    - Редактировать карточку (`PUT /api/masters/:id`) может либо руководитель, либо сам мастер в пределах своего профиля.
    - При смене собственного пароля мастер мгновенно получает обновлённый JWT-токен без разлогина сессии.
- **Хранилище метаданных сотрудников (`settings.data.masters_meta`)**:
  - Расширенные свойства профиля (роль, телефон, специализация, заметки) безопасно синхронизируются в JSONB-структуре настроек Supabase, обеспечивая расширяемость схемы без деструктивных DDL-миграций и 100% совместимость с оффлайн-режимом `data/data.json`.

## [1.4.0] - 2026-09-19

### 🚀 Feature: Интеграция полного каталога из 22 услуг автоэлектрика (г. Алчевск)

- **База данных Supabase & Fallback (`data/data.json`)**:
  - Загружен и структурирован полный каталог из 22 специализированных услуг автоэлектрика на базе маркетингового анализа рынка Алчевска и агломерации ЛНР (`alchevsk_autoelectric_price_and_market.md`).
  - Всем позициям присвоены уникальные последовательные `sort_order` от 0 до 21, профессиональные описания, рыночные цены региона и семантические иконки Lucide.
  - Охвачены 8 ключевых направлений:
    1. Компьютерная диагностика ЭБУ и систем (чтение, расшифровка, Live Data) — от 800 ₽;
    2. Диагностика мотор-тестером и осциллографом (ДПКВ/ДПРВ, фазы, зажигание) — от 1 500 ₽;
    3. Поиск утечки тока и разряда АКБ — от 1 000 ₽;
    4. Поиск обрыва цепи и короткого замыкания (КЗ) — от 1 000 ₽;
    5. Снятие, дефектовка и ремонт генератора — от 1 500 ₽;
    6. Снятие, дефектовка и ремонт стартера — от 1 500 ₽;
    7. Восстановление кос и жгутов проводки — от 1 500 ₽;
    8. Замена и перепиновка разъемов датчиков и фар — от 500 ₽;
    9. Замена монтажных блоков и силовых проводов — от 1 200 ₽;
    10. Диагностика и ремонт системы зажигания — от 500 ₽;
    11. Замена погружного бензонасоса и реле топлива — от 1 200 ₽;
    12. Замена датчиков двигателя и кислорода (лямбда) — от 600 ₽;
    13. Ремонт светотехники, фар и установка ПТФ/ДХО — от 600 ₽;
    14. Ремонт стеклоподъемников и моторчиков дворников — от 1 200 ₽;
    15. Ремонт мотора отопителя и контактной группы замка — от 1 000 ₽;
    16. Установка сигнализаций с автозапуском — от 4 500 ₽;
    17. Аварийное отключение и демонтаж сигнализаций — от 1 800 ₽;
    18. Установка автомагнитол Android, камер и парктроников — от 1 500 ₽;
    19. Скрытое подключение регистраторов и фаркопов — от 1 000 ₽;
    20. Прошивка ЭБУ (Евро-2 / Чип-тюнинг) и Immo OFF — от 2 500 ₽;
    21. Выездной запуск двигателя («прикурить» 12V) — от 600 ₽;
    22. Выездной комплекс «Не заводится» — от 2 000 ₽.
- **CRM и витрина сайта**:
  - Все 22 услуги доступны в разделе «Услуги» панели мастера с возможностью мгновенного редактирования цен, названий и перетаскивания.
  - На главной витрине [`чекгорит.рф`](https://чекгорит.рф/) услуги отображаются в адаптивной сетке с иконками Lucide.

## [1.3.5] - 2026-09-19

### 🐛 Fix: Фиксация порядка услуг (`sort_order`) при редактировании в CRM

- **Supabase**:
  - Нормализован `sort_order` услуг (`id: 1` → 0, `id: 2` → 1, `id: 3` → 2, `id: 4` → 3) и отзывов. Устранено перемещение обновляемых записей в конец таблицы из-за коллизий нулевых значений и поведения PostgreSQL MVCC.
- **Backend (`api/index.js`)**:
  - `POST /api/services`: при редактировании существующей услуги её `sort_order` сохраняется; при создании новой услуги автоматически вычисляется следующий порядковый номер (`max(sort_order) + 1`).
  - Во всех выборках услуг и отзывов (`GET /api/data`, `GET /api/services`, `POST /api/services`, `PUT /api/services/reorder`, `DELETE /api/services/:id`, `PUT /api/reviews/reorder`) добавлен детерминированный вторичный ключ сортировки `.order('id', { ascending: true })`.
- **CRM UI (`public/js/admin.js`)**:
  - В `openServiceModal`: скрытый инпут `sort_order` корректно считывает сохранённый порядок услуги (с фоллбэком на текущую позицию в массиве). При создании новой услуги `sort_order` устанавливается в конец списка (`DATA.services.length`).
  - В `window.onDrop`: после сохранения нового порядка через `PUT /api/services/reorder` локальный массив `DATA.services` обновляется данными из ответа сервера для синхронизации новых индексов в памяти.
  - В `window.toggleService`: переключение видимости услуги сохраняет исходный `sort_order` и обновляет `DATA.services` из ответа API.

## [1.3.4] - 2026-09-19

### 🐛 Fix: Toast-уведомления теперь видны поверх модальных окон

- **`public/admin.html`**: z-index тоста `#toast` поднят с `z-50` до `z-[9999]`, чтобы уведомления об ошибках и успехе отображались поверх любых диалоговых окон (форм клиентов, мастеров, ремонтов и т.д.).

## [1.3.3] - 2026-09-19


### 🛡️ Security: Absolute Deletion Protection for Lead Master Accounts

- **Backend Protection (`DELETE /api/masters/:id`)**:
  - Внедрена жесткая защита от удаления системных аккаунтов `Sid Vicious` (`vk_1125744855`) и `Seryozhka Abisonov` (`vk_250130315`).
  - При любой попытке удаления через API возвращается `403 Forbidden` (`Этот аккаунт мастера защищён от удаления`).
- **CRM UI (`public/js/admin.js`)**:
  - Для защищённых профилей удалена кнопка корзины.
  - Добавлены бейджи `Lead` и `🛡️ Защищён`.
  - В `deleteMaster()` добавлена клиентская валидация.
- **Supabase**:
  - Восстановлена учетная запись мастера `Seryozhka Abisonov`.

## [1.3.2] - 2026-09-19

### 🔐 Fix: Master Authentication & Dual-Mode Login

- **Backend Auth (`POST /api/auth`)**:
  - Восстановлена поддержка глобального мастер-пароля из настроек (`settings.data.password` / `'admin'`) для входа без логина или с логином мастера.
  - В таблице `masters` Supabase актуализированы bcrypt-хэши паролей для учетных записей `vk_1125744855` (Sid Vicious) и `vk_250130315` (Seryozhka Abisonov).
  - Теперь успешно работают оба сценария:
    1. Вход только по паролю (`admin`);
    2. Вход по связке логин (`vk_1125744855`) + пароль (`admin`).

## [1.3.1] - 2026-09-19

### 🐛 Fix & Optimization: Asset Versioning, Cache Invalidation & PIN Code Display

- **Cache-Busting & Asset Versioning**:
  - В `public/admin.html`, `public/profile.html` и `public/index.html` добавлена версионность скриптов (`?v=1.3.1`), предотвращающая загрузку браузерами устаревших версий `admin.js`, `profile.js` и `main.js` из локального дискового кэша.
  - В `vercel.json` настроены заголовки `Cache-Control: no-cache, no-store, must-revalidate` для скриптов `/js/*` и HTML-страниц для моментального применения обновлений без необходимости принудительного сброса кэша (Ctrl+F5).
- **CRM Frontend (public/js/admin.js)**:
  - Исправлен текст тоста при создании нового клиента: теперь корректно отображается `ПИН: ${res.client.pin}` вместо устаревшего `accessCode`.
  - Добавлена мета-инструкция кэш-контроля в заголовки страниц.

## [1.3.0] - 2026-09-19

### 🚀 Major Feature: 5-Level Omnichannel Client Identity & Zero-Loss Repair Linking

Реализована комплексная система сквозной идентификации клиентов («Инкогнито» ➔ «Профиль»), исключающая потерю истории ремонтов и сервисных книжек:

#### 1. Бэкенд Smart-Merge (Умное объединение дубликатов):
- **api/index.js**:
  - Добавлена функция `mergeClientAccounts(targetId, sourceId)`: автоматическое объединение профилей по номеру телефона при входе через Telegram, VK или форму профиля.
  - Объединяет историю ремонтов (`repairs` с дедупликацией по id), автомобили (`cars`), сессии (`auth_sessions`) и привязывает соцсети (`telegram_id`, `telegram_chat_id`, `vk_id`).
  - В эндпоинте `POST /api/client/profile/phone` при сохранении недостающего телефона выполняется авто-слияние с профилем мастера и выдача обновленной сессии.

#### 2. Telegram Bot: One-Tap подтверждение контакта (`request_contact`):
- **api/index.js**:
  - В вебхуке `/api/telegram-webhook` и polling-режиме добавлена обработка нативного контакта `update.message.contact` (`msg.contact.phone_number`).
  - При входе `/start auth_<session>` для новых клиентов бот отправляет нативную кнопку `[ 📱 Поделиться номером для поиска авто ]`.
  - При нажатии бот получает проверенный телефон, связывает аккаунты через Smart-Merge и моментально одобряет веб-сессию.

#### 3. Прямой доступ (Magic Link) для клиента из CRM:
- **api/index.js**:
  - Добавлен эндпоинт `GET /api/clients/:id/magic-link`, генерирующий подписанный токен прямого доступа к профилю клиента без ввода логинов и паролей.
- **public/js/admin.js & public/admin.html**:
  - В карточке клиента и деталях добавлены кнопки: `📋 Скопировать ссылку`, `💬 В WhatsApp` и `✈️ В Telegram` с готовым текстом приглашения в личный кабинет.
- **public/js/profile.js**:
  - Добавлена обработка query-параметра `?auth=TOKEN`: токен сохраняется в `localStorage`, очищается адресная строка, и клиент сразу видит свою сервисную книжку.

#### 4. Мгновенная гостевая сессия при отправке заявки на лендинге:
- **api/index.js**:
  - В `POST /api/requests` автоматически создаётся профиль клиента по указанному телефону (если не был создан ранее) и генерируется токен сессии.
- **public/js/main.js & public/index.html**:
  - При отправке заявки сессионный токен сохраняется в `localStorage`.
  - Модальное окно успеха отображает красивую карточку с кнопкой `[ 🚗 Открыть личный кабинет ]`, позволяя клиенту сразу перейти в свой профиль.

#### 5. Авторизация по номеру телефона и персональному 4-значному ПИН-коду:
- **api/index.js**:
  - Добавлена функция `getClientPin(phone, clientId)`: детерминированный криптографический 4-значный ПИН-код на основе HMAC-SHA256, не требующий сторонних SMS-шлюзов и миграций БД.
  - Добавлен защищённый рейт-лимитером эндпоинт `POST /api/client/auth/pin` (макс. 10 попыток за 15 минут).
  - В `GET /api/clients`, `GET /api/clients/:id` и `GET /api/client/me` включено поле `pin`.
- **public/profile.html & public/js/profile.js**:
  - Добавлена форма входа по номеру телефона и 4-значному ПИН-коду с валидацией маски телефона и защитой от брутфорса.
- **public/js/admin.js**:
  - ПИН-код отображается в карточках списка клиентов и в модальном окне деталей с кнопкой быстрого копирования для передачи клиенту.

## [1.2.5] - 2026-09-19

### 🐛 Critical Fix: Client On-the-fly Creation in CRM (`POST /api/clients` 500 Fix)

#### api/index.js:
- **[DATABASE/SUPABASE] Исправление схемы запроса к таблице `clients`**:
  - Устранена критическая ошибка 500 (`Could not find the 'accessCode' column of 'clients' in the schema cache`), возникавшая в Supabase PostgREST при попытке создания клиента из модального окна добавления ремонта.
  - Из объекта вставки `payload` в `POST /api/clients` удалено поле `accessCode`, отсутствующее в реальной структуре таблицы `clients`.

#### public/js/admin.js:
- **[CRM UX] Информативные уведомления об ошибках**:
  - В обработчике сохранения ремонта `bindRepairForm` улучшен вывод тостов при сбое создания профиля клиента: теперь отображается точное серверное сообщение об ошибке (`cRes?.error || cRes?.message`), предотвращая скрытые сбои без понятной диагностики.

## [1.2.4] - 2026-09-19

### 🎯 CRM UX Polish: Searchable Client Combobox, Prominent Dates & Red Pulsing Badge

#### public/admin.html:
- **[CRM UI] Красный пульсирующий бейдж новых заявок**:
  - Добавлена анимация `@keyframes badge-pulse` с мягким пульсирующим масштабированием и красным свечением.
  - Бейдж `#new-requests-badge` получил класс `badge-pulse` с цветом `#ef4444` и белым жирным шрифтом. Он больше не сливается с активной синей вкладкой «Заявки» и отчётливо виден из любой вкладки CRM.
- **[CRM UX] Searchable Client Combobox в модальном окне ремонта**:
  - Поле выбора клиента переработано из статичного выпадающего списка в быстрый поисковый комбобокс (`#repair-client-search`, `#repair-client-dropdown`, `#repair-client-clear-btn`).
  - Добавлен счётчик клиентов в базе (`#repair-client-count`).

#### public/js/admin.js:
- **[CRM REACTIVITY] Универсальная функция `updateRequestsBadge(count)`**:
  - Бейдж новых заявок синхронизируется в реальном времени при старте (`loadAndShow`), при переключении статусов заявок (`setRequestStatus`), при удалении (`deleteRequest`) и при загрузке аналитики (`loadAnalytics`).
  - При наличии новых заявок бейдж пульсирует красным, а при их отсутствии (0) автоматически скрывается.
- **[CRM DATA] Полноценное отображение даты и времени заявок**:
  - В виджете «Последние заявки» на вкладке «Обзор» добавлено отображение даты и времени с иконкой часов (`fmtDateTime(r.createdAt || r.created_at)`).
  - В карточках раздела «Заявки» дата вынесена в контрастный бейдж с иконкой календаря (`📅 Заявка от: ...`), устранён нечитаемый цвет `text-gray-600`.
- **[CRM UX] Поиск клиентов в модалке ремонта**:
  - Интерактивный поиск по имени и номеру телефона в реальном времени, с поддержкой спец-действий (`✨ Создать из заявки`, `➕ Создать вручную`), кнопкой быстрой очистки поля (✕) и закрытием по клику вне области.
  - Совместимость с автосозданием клиента из заявки (`__NEW_FROM_REQUEST__`).

## [1.2.3] - 2026-09-19

### 📱 Critical Fix: Mobile Request Modal, Global Event Handlers & Overlay Isolation

#### public/index.html:
- **[MOBILE UX] Мгновенный вызов модалки заявки**:
  - Кнопкам `#hero-request-btn` и `#sticky-request-btn` добавлены прямые обработчики `onclick="window.openRequestModal ? window.openRequestModal() : null"` и `type="button"`.
  - Модальному окну `#modal-request` задан гарантированный стиль `style="display: none; z-index: 99999;"`, предотвращающий конфликт с Tailwind CDN и скрытие под другими слоями.
  - Кнопке закрытия `#close-request-modal` добавлен прямой вызов `onclick="window.closeRequestModal ? window.closeRequestModal() : null"`.
- **[PERF/STABILITY] Удалена отладочная консоль `vConsole` из `<head>`**:
  - Устранен блокирующий синхронный запрос к `unpkg.com`, вызывавший ошибку `TypeError: window.VConsole is not a constructor` и сбои рендеринга на мобильных устройствах при сетевых задержках/блокировках.
- **[STABILITY] Перевод Lucide CDN на jsdelivr**:
  - Заменен ненадежный в РФ `unpkg.com` на стабильный `cdn.jsdelivr.net`.

#### public/js/main.js:
- **[ARCHITECTURE] Глобальные и немедленные функции модалки**:
  - `window.openRequestModal` и `window.closeRequestModal` теперь объявляются в глобальной области видимости сразу при загрузке скрипта, а не внутри отложенной `async function init()`.
  - Добавлено глобальное делегирование событий клика и тапа (`document.addEventListener('click', ...)`), гарантирующее открытие модалки даже до завершения фонового запроса `/api/data`.
  - Глобализован обработчик отправки формы `#form-public-request`.
- **[RESILIENCE] Отказоустойчивость `init()`**:
  - Все блоки инициализации (`renderHero`, `renderServices`, `renderReviews`, `renderContacts`, `lucide.createIcons`, `initNavbar`, `initAnimations`, `initLightbox`, `initCalculator`) обернуты в индивидуальные `try...catch`.
  - Инициализация `new Swiper` защищена проверкой существования библиотеки `typeof Swiper !== 'undefined'`. Никакой сбой сторонних CDN больше не прерывает работу сайта.

#### public/js/perf-widget.js:
- **[MOBILE UX] Изоляция виджета производительности**:
  - Добавлено ограничение `if (window.innerWidth < 768 || /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)) return;`. Виджет больше не рендерится на мобильных устройствах с `z-index: 99999` и не перекрывает кнопки нижней фиксированной планки (`#sticky-cta`).

## [1.2.2] - 2026-09-19

### 🛡️ Full Auditor Resolution: Security, Loyalty Synchronization & CRM Reactivity

#### api/index.js:
- **[SECURITY] Защита токена Telegram-бота (`PUT /api/settings`)**:
  - Токен бота больше не затирается пустой строкой `""` при сохранении общих настроек (контакты, имя мастера и т.д.). Поллинг бота не обрывается.
- **[SECURITY] Санитизация sessionCode в Telegram Webhook**:
  - Предотвращена PostgREST Syntax Injection через `/start auth_<sessionCode>` с помощью валидации `/^[a-zA-Z0-9_-]{1,64}$/`.
- **[PERF/STABILITY] DoS/Rate Limit фикс для Telegram Magic Link**:
  - Добавлен выделенный лимитер `limiterTgMagicStatus` (200 запросов / 5 мин) для эндпоинта `/api/client/auth/telegram/magic/status`, устранена ложная блокировка 429 при поллинге статуса входа.
- **[CRM] Защита от самоудаления мастера**:
  - `DELETE /api/masters/:id` блокирует удаление текущего авторизованного мастера (`req.masterId`).
- **[CRM] Сохранение порядка сортировки услуг**:
  - `POST /api/services` корректно принимает и сохраняет `sort_order` (поддерживая оба формата: `sort_order` и `sortOrder`).
- **[ANALYTICS] Корректный расчет оттока и исключение напоминаний**:
  - Синхронизирован критерий оттока клиентов с интерфейсом на 180 дней (6 месяцев).
  - В массив `churnClients` добавлен `id: c.id`.
  - Записи с `type: 'Напоминание'` исключены из подсчёта визитов, выручки за месяц, частотности услуг и анализа оттока, а также исключены из расчета уровня клиента в `/api/client/me`.
- **[PROXY] Добавлен `app.set('trust proxy', 1)`** для корректной работы rate limiting за обратным прокси.

#### public/admin.html & public/js/admin.js:
- **[SECURITY] Защита токена бота в настройках**:
  - Форма `#form-settings` не отправляет пустое поле токена, если мастер его не менял.
- **[CRM] Сохранение порядка сортировки услуг**:
  - В `#form-service` добавлен скрытый инпут `sort_order`, функции `toggleService`, `openServiceModal` и `bindServiceForm` сохраняют исходную позицию при изменении активности и параметров.
- **[CRM] Реактивная аналитика (`loadAnalytics`)**:
  - После любых изменений ремонтов (добавление, удаление), создания клиентов и перевода заявок аналитика автоматически обновляется без перезагрузки страницы.
- **[CRM UX] Сохранение полей формы ремонта**:
  - При выборе «Создать клиента» из формы добавления ремонта введенные сумма и описание больше не стираются; при обнаружении существующего клиента форма автоматически выбирает его.
- **[CRM UX] Карточка клиента не закрывается при добавлении ремонта**:
  - После сохранения ремонта обновляется карточка клиента и список ремонтов, модальное окно клиента остаётся открытым.
- **[CRM UX] Добавлена кнопка возврата заявки в «Новые»**:
  - Мастер может вернуть заявку в статус `status: 'new'`, бейдж счетчика новых заявок и overview пересчитываются динамически.
- **[CRM UI] Уровень клиента в карточке**:
  - Добавлен расчет уровня в `renderClientDetail` с поддержкой RU и EN ключей в `levelBadge`.
- **[BUGFIX] Устранены двойные слушатели `[data-goto]`**.
- **[BUGFIX] Безопасный просмотр фото отзывов**:
  - Замена инлайн-вызова `window.open` на дата-атрибут `data-src` и делегирование в `openReviewImage` (устранены ошибки кавычек в URL).
- **[BUGFIX] Корректный логаут**:
  - Возврат на `#login-screen` с очисткой токена вместо ошибочного редиректа в клиентский профиль.
- **[BUGFIX] Автоматический редирект на экран входа при 401 Unauthorized** в `api()`.
- **[BUGFIX] Устранен сброс таймера в `toast()`.**
- **[HTML] Закрыт незакрытый тег `<div>` в `admin.html`.**

#### public/profile.html & public/js/profile.js:
- **[LOYALTY] Синхронизация шкалы лояльности**:
  - Клиентский портал переведен на 4-уровневую систему бэкенда (`Новый` [0, 2], `Лояльный` [2, 5], `Постоянный` [5, 10], `VIP` [10+]).
  - Полностью устранён баг с отрицательным прогрессом (-33%) и текстом "-1 / 3 визитов".
  - В `profile.html` добавлен CSS-бейдж `.level-loyal`.
- **[DATA INTEGRITY] Исключение напоминаний ТО из статистики визитов**:
  - Напоминания больше не накручивают визиты и не влияют на расчет среднего чека в профиле клиента.
- **[AUTH UX] Оптимизация поллинга Telegram Magic Link**:
  - Интервал увеличен с 2000 мс до 2500 мс (не более 120 запросов за сессию при лимите 200).
  - Добавлена безопасная обработка статуса 429.
- **[UX] Информативная валидация телефона при создании заявки из профиля**:
  - Если у клиента не указан телефон, отображается четкое руководство с фокусом на баннер добавления номера вместо неинформативной ошибки «Ошибка. Позвоните мастеру».

## [1.2.1] - 2026-09-19

### 🎯 Full Auditor Consolidation & CRM Frontend/Auth Polish

#### public/admin.html & public/js/admin.js:
- **[CRITICAL BUG] Восстановлена форма входа в панель мастера**:
  - Устранено критическое несоответствие: `admin.js` ожидал `#auth-btn`, `#auth-username`, `#auth-password`, которых не было в `admin.html`. Из-за этого при клике на «Войти» форма нативно перезагружала страницу без авторизации.
  - В `admin.html` добавлено опциональное поле логина (`#login-user`) и поле пароля (`#login-pwd`), форма привязана к `#login-form`.
  - В `admin.js` обработчик переведен на `form.addEventListener('submit')` с `e.preventDefault()`, чтением логина/пароля и выводом ошибок в `#login-err`.

#### api/index.js:
- **[FEATURE] Гибкая авторизация мастера (`POST /api/auth`)**:
  - Поддержка входа как по паре `(username, password)`, так и по одному только `password` (с перебором зарегистрированных мастеров по bcrypt).
- **[CRM] Клиенты и дубликаты (`POST /api/clients`)**:
  - Добавлена проверка на дубликаты по телефону (`client_exists`), предотвращающая дублирование профилей.
  - Добавлена генерация 6-значного `accessCode` — в тосте фронтенда теперь выводится реальный код (`Клиент создан. Код: 123456`), а не `undefined`.
  - Добавлена нормализация телефона (`normalizePhone`).
  - В `GET /api/clients` добавлена обработка ошибок Supabase.
- **[OTP Auth] Нормализация телефона в клиентском портале**:
  - В `POST /api/client/auth/request` и `POST /api/client/auth` подключена нормализация `normalizePhone` с поиском по нормализованному и исходному форматам.

#### public/js/profile.js:
- **[CLEANUP] Удален спам 404 к `/api/debug`**:
  - В обработчиках ошибок `window.onerror` и `unhandledrejection` вызовы несуществующего `/api/debug` заменены на `console.error`.

## [1.2.0] - 2026-09-18

### 🔒 Critical Security & Stability Fixes (Backend API Audit)

#### api/index.js — критические исправления:
- **[SEC] Rate Limit** `POST /api/requests`: подключён `limiterPublic` (30 req/min) — закрыта уязвимость DoS/спам-заявок.
- **[SEC] Telegram HTML-инъекция**: добавлена функция `escapeTgHtml()`, применена в уведомлениях бота — спецсимволы `<>&` больше не ломают parse_mode HTML.
- **[BUG] `createdAt` маппинг в заявках**: `GET /api/requests` и `PUT /api/requests/:id/status` теперь возвращают `createdAt: r.created_at` — даты заявок корректно отображаются в admin.js.
- **[BUG] `.single()` → `.maybeSingle()`**: исправлено в 4 местах (`PUT /api/reviews/:id`, `POST /api/masters`, `POST /api/clients/:id/repairs`, `DELETE /api/clients/:id/repairs/:rid`) — устранены аварийные падения с PGRST116 при отсутствии записи.
- **[BUG] Вход мастера через Telegram**: в `/api/client/me` добавлена проверка `telegram_chat_id` при определении мастерских прав — кнопка «Панель Мастера» теперь появляется при входе через Telegram.
- **[BUG] Смена пароля мастера** (`PUT /api/settings`): пароль теперь bcrypt-хешируется и сохраняется в таблице `masters` (не в `settings`); возвращается новый JWT токен — не теряется доступ к админке.
- **[BUG] Ложный разлогин клиентов**: `clientAuth` теперь отличает ошибки БД (`503`) от истёкших сессий (`401`) — не разлогинивает клиентов при временной недоступности Supabase.
- **[NEW] `GET /api/services`** (authCheck): защищённый эндпойнт возвращает все услуги, включая неактивные — деактивированные услуги больше не исчезают из панели администратора.
- **[BUG] Аналитика выручки**: `Number(r.cost)` вместо `typeof === 'number'` — строки-числа (`"500"`) теперь корректно суммируются.
- **[BUG] Churn-анализ**: последний ремонт определяется сортировкой по дате, а не позицией в массиве.

#### public/js/admin.js:
- **[BUG] Смена пароля**: устранена критическая ошибка — токен больше не заменяется на plain-text пароль; используется новый JWT из ответа сервера.
- **[BUG] Услуги**: `loadAndShow()` теперь загружает услуги через защищённый `GET /api/services` (все, включая неактивные).

#### public/js/profile.js:
- **[BUG] Ложный разлогин**: при ответе 503 (ошибка БД) токен клиента больше не очищается из localStorage.

## [1.1.9] - 2026-09-18

### 👥 CRM Clients List Data Sync & Display Fix
- **Исправление рассинхронизации списка клиентов (`clientsSummary` -> `clients`)**:
  - Устранена критическая ошибка, из-за которой на вкладке «Обзор» отображалось корректное количество клиентов («Всего клиентов: 4»), но на вкладке «Клиенты» выводилось сообщение «Клиентов пока нет».
  - Функция `renderClients()` и `loadClients()` синхронизированы для работы с общим состоянием `DATA.clients`, которое наполняется при стартовой загрузке страницы `loadAndShow()`.
- **Улучшенное отображение контактов и метаданных**:
  - Добавлена поддержка вывода контактов без номера телефона (Telegram username или VK ID).
  - Добавлены бейджи привязки соцсетей (`✈ TG`, `VK`) в списке клиентов.
  - Динамический расчет уровня клиента (Новичок, Постоянный, Ветеран) и цветовой подсветки зоны оттока (Retention).
  - Расширен живой поиск клиентов: теперь фильтрация работает не только по имени и телефону, но и по Telegram username, VK ID и данным автомобилей.

## [1.1.8] - 2026-09-18

### 🛠️ Request-to-Repair Workflow Fix & CRM Client Selection
- **Исправление ошибки «Выберите клиента» при завершении заявки**:
  - В модальное окно «Запись о ремонте» (`modal-repair`) добавлен выпадающий список выбора клиентов (`#repair-client-select`).
  - При переводе заявки в статус «Выполнена» система автоматически ищет клиента по номеру телефона из заявки. Если клиент найден — он сразу предвыбирается с подгрузкой его автомобилей.
  - Если клиент новый — первой строкой выпадающего списка предлагается `✨ Создать клиента: [Имя] ([Телефон])`. При сохранении записи профиль клиента создается автоматически, и ремонт надежно привязывается к нему.
  - Описание неисправности из заявки автоматически переносится в поле описания работ ремонта.
- **Быстрое добавление авто из окна ремонта**:
  - Добавлена кнопка `+ Добавить авто клиенту` непосредственно в форму создания ремонта без необходимости переходить в карточку клиента.
- **Управление клиентами в CRM**:
  - В карточку клиента добавлено отображение привязки соцсетей (Telegram / VK ID) и кнопка `Удалить клиента из базы` с подтверждением.

## [1.1.7] - 2026-09-18

### 👨‍🔧 Master Accounts Provisioning & Seamless Admin Panel Access
- **Очистка тестовых аккаунтов**: Полностью удалены временные тестовые аккаунты администраторов из базы данных Supabase (`masters`).
- **Назначение боевых аккаунтов Мастеров**:
  - В роли Мастеров с полными правами администратора зарегистрированы 2 реальных профиля: `Sid Vicious` (`vk_id: 1125744855`, Telegram Chat ID: `6208918931`) и `Seryozhka Abisonov` (`vk_id: 250130315`).
- **Бесшовный вход в Панель Мастера (`/admin.html`)**:
  - В `/api/client/me` добавлено распознавание профилей Мастеров при авторизации через VK ID или Telegram. При входе Мастера бэкенд автоматически генерирует и возвращает валидный подписанный `adminToken` (JWT со сроком 30 дней).
  - В интерфейсе личного кабинета (`profile.html`) для мастеров активируется кнопка `Панель Мастера`.
  - При клике на `Панель Мастера` открывается панель управления CRM (`admin.html`) без повторного запроса пароля со всеми доступными инструментами администратора (заявки, клиенты, ремонтные записи, аналитика, настройки).
  - В шапку `admin.html` добавлена быстрая ссылка `Профиль` для мгновенного переключения обратно в личный кабинет.

## [1.1.6] - 2026-09-18

### ⚡ Diagnostic Performance HUD & Loading Analysis
- **Встроенный счётчик производительности (`perf-widget.js`)**: Добавлен ненавязчивый плавающий инспектор скорости загрузки страниц в левом нижнем углу (`⚡ 0.85s`), с детальной раскладкой по клику (TTFB сервера, время парсинга DOM, сетевые задержки DNS/TLS, вес страницы и топ самых медленных ресурсов).
- **Консольная телеметрия**: Детальная таблица `console.table()` со всеми этапами загрузки по Navigation Timing API автоматически пишется в DevTools.
- **Оптимизация критического пути рендеринга (`profile.html`)**:
  - Убран блокирующий 302-редирект от `lucide@latest`, библиотека зафиксирована на точной версии `lucide@0.468.0` с атрибутом `defer`.
  - Добавлен атрибут `defer` на внешний SDK VK ID (`@vkid/sdk@2.6.8`), предотвращающий зависание парсера HTML.
  - Настроены preconnect-соединения для шрифтов Google Fonts.

## [1.1.5] - 2026-09-18

### 🛡️ Cross-Browser Origin Stabilization & Direct VK ID Flow
- **Устранение «В настройках ресурса не указан данный базовый домен»**: Заменен вызов `VKID.Auth.login()` в кнопке входа на прямой серверный маршрут `/api/client/auth/vk/login`. Это полностью исключает передачу несовместимого `origin` из сторонних браузеров и доменов (например, `auto-electrician-landing.vercel.app` или ненормализованной кириллицы), гарантируя отправку валидного Punycode-ориджина `https://xn--c1adkgvmp7a.xn--p1ai`.
- **Автоматическая нормализация домена**: Клиенты, открывшие сайт по техническому адресу Vercel, автоматически перенаправляются на канонический боевой домен `чекгорит.рф`.

## [1.1.4] - 2026-09-18

### 🚀 VK ID Serverless PKCE & Redirect URL Alignment
- **Синхронизация Redirect URL с VK ID Console**: Устранена критическая причина «Ошибка загрузки» (код `redirect_uri is missing or invalid`), вызванная несовпадением зарегистрированного в консоли VK адреса. В `profile.js` и бэкенде установлен зарегистрированный адрес `https://auto-electrician-landing.vercel.app/api/client/auth/vk/callback`.
- **Бессерверный (Stateless) PKCE-протокол**: Заменено хранилище `vkPkceStore` (in-memory Map) на криптографически подписанный HMAC-SHA256 токен состояния `state`, содержащий верификатор `code_verifier` и целевой URL возврата. Это полностью предотвращает потерю контекста при редиректах между различными бессерверными контейнерами Vercel (Cold Starts).
- **Бесшовный возврат на кастомный домен**: После прохождения авторизации через VK ID бэкенд автоматически перенаправляет пользователя обратно на исходный домен (`чекгорит.рф` / `xn--c1adkgvmp7a.xn--p1ai`) с сессионным токеном.

## [1.1.3] - 2026-09-18

### 🛠️ VK ID 2.0 PKCE & SDK Hardening
- **Устранение «Ошибка загрузки» (Код 9 VK ID)**: Заменен устаревший вызов `oauth.vk.com/authorize` на современный протокол VK ID OAuth 2.1 с генерацией PKCE (`code_challenge`, `code_challenge_method=s256`, `state`) через эндпоинт `id.vk.ru/auth`.
- **Клиентский вызов VKID.Auth.login()**: Кнопка входа «Войти через ВКонтакте» теперь вызывает нативный метод SDK `VKID.Auth.login()` со встроенной генерацией PKCE-параметров, открывая форму входа без редиректа на устаревший интерфейс.
- **Стабилизация загрузки SDK**: Фиксирована точная версия скрипта `@vkid/sdk@2.6.8` (убран спецсимвол `<` из тега `src`, вызывавший задержки и 302-редиректы CDN).
- **Синхронизация Redirect URI**: Строго зафиксирован `redirectUrl: 'https://чекгорит.рф/api/client/auth/vk/callback'` в точном соответствии с настройками панели VK ID.

## [1.1.2] - 2026-09-18

### ⚡ VK ID One Tap & OAuth Integration
- **VK ID One Tap Web SDK**: Внедрен официальный виджет быстрого входа в один клик (`@vkid/sdk` v2) в личном кабинете (`profile.html`), обеспечивающий бесшовную авторизацию без перехода на сторонние экраны.
- **Серверная валидация токенов**: Реализован эндпоинт `POST /api/client/auth/vk` с верификацией токенов через VK ID API (`https://id.vk.com/oauth2/user_info`), защитой от брутфорса (`limiterVkAuth`) и автоматической связкой профиля в Supabase (`clients.vk_id`).
- **Резервный серверный OAuth маршрут**: Добавлен эндпоинт `GET /api/client/auth/vk/login` и обновлен `GET /api/client/auth/vk/callback` с динамическим распознаванием хоста (`чекгорит.рф` / Punycode) и защитой от сетевых блокировок виджета.
- **Отказоустойчивость**: Добавлены безопасные фоллбеки для `VK_APP_ID`, `VK_APP_SECRET`, `VK_SERVICE_TOKEN` и `VK_REDIRECT_URI`.

## [1.1.1] - 2026-09-14

### 🔐 Telegram Auth & Security Hardening
- **Восстановление реакции кнопки входа**: устранена проблема тихого сбоя кнопки при недоступности или задержке инициализации сессии. Кнопка теперь имеет активный обработчик клика, состояние загрузки со спиннером и явное уведомление об ошибках.
- **HMAC-SHA256 валидация для Telegram Widget**: внедрен эндпоинт `POST /api/client/auth/telegram` с криптографической проверкой подписи `hash` через `crypto.timingSafeEqual` по спецификации безопасности Telegram Login Widget.
- **Защита от DoS и Brute-force**: эндпоинты авторизации Telegram защищены специализированным рейт-лимитером `limiterTgAuth` (максимум 30 запросов в 5 минут).
- **Криптографическая стойкость сессий**: энтропия одноразовых сессий Magic Link увеличена до 256 бит (`crypto.randomBytes(32)`).
- **Диагностика состояния БД (Supabase)**: внедрена явная классификация ошибок соединения с базой (код `db_paused` при статусе паузы в Supabase free-tier) с понятным сообщением пользователю вместо непрозрачного HTTP 500.
- **Очистка устаревших вызовов**: удалены обращения к закрытому тестовому маршруту `/api/debug` из клиентских скриптов.

## [1.1.0] - 2026-09-10

### 🛡️ Security & Architecture (Аудит Astra / OpenRouter Nemotron 120B Super)
- **Унификация бэкенда**: `server.js` преобразован в модульный раннер поверх `api/index.js`, устранено дублирование эндпоинтов и расхождение контрактов между локальной средой и Vercel.
- **Устранение утечки секретов (SEC-01)**: удалены открытый мастер-пароль `admin`, токен Telegram бота и личный Chat ID из `data/data.json`. Добавлен `.env.example` с шаблоном переменных окружения.
- **Изоляция CRM и публичного API (SEC-02)**: реализован строгий DTO-фильтр `publicLandingDto` в `GET /api/data`. Клиентская база и заявки больше никогда не отдаются неавторизованным пользователям.
- **Защита Telegram Webhook (SEC-03)**: добавлена проверка заголовка `X-Telegram-Bot-Api-Secret-Token` с использованием `crypto.timingSafeEqual` против timing attacks.
- **Устранение privilege escalation в профиле (SEC-04, SEC-05)**: метод `GET /api/client/me` больше не выдает JWT администратора; `GET /api/client/profile` больше не возвращает мастер-пароль.
- **XSS-санитизация (SEC-07)**: в `admin.js` и `profile.js` обновлена функция `esc()` для экранирования апострофов и обратных кавычек, добавлена функция `sanitizePhone()` для безопасных ссылок `tel:`.
- **Защита эндпоинтов и маршрутизация (SEC-08)**: удален открытый эндпоинт `/api/debug`, закрыт логгер `/api/logs` с `authCheck`, исправлен конфликт маршрутов `PUT /api/reviews/reorder` и `PUT /api/reviews/:id`. Добавлены недостающие маршруты управления автомобилями клиента (`POST /api/clients/:id/cars`, `DELETE /api/clients/:id/cars/:cid`). Сохранены поля напоминаний о ТО (`reminderDate`, `reminderText`).

### 🎨 UI/UX & Living Motion (OpenRouter UI/UX Agent)
- **Живой статус мастера**: внедрен неоновый статус-индикатор мастера с круговой радарной пульсацией CSS (`@keyframes pulseRadar`) в Hero-блоке, адаптирующийся к статусу приема заявок и поддержкой `prefers-reduced-motion`.
- **Экспресс-калькулятор неисправностей**: разработан интерактивный 3-шаговый калькулятор поломок (категория авто, симптом, ориентировочная стоимость и время прибытия) с прямой интеграцией в модальное окно заявки.
- **Маскот «Электрон» по запросу**: отключен навязчивый принудительный онбординг при загрузке страницы. Маскот переведен в режим деликатного помощника-диагноста по клику с быстрыми чипами неисправностей.
- **Разделение формы и маскота**: отправка формы заявки (`/api/requests`) вынесена в `main.js`, гарантируя 100% работоспособность оформления вызова независимо от декоративного маскота.
- **Очистка репозитория**: удалены устаревшие файлы миграций, дампы логов и тяжелые тестовые изображения.
